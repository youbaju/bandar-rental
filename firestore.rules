rules_version = '2';

// قواعد أمان قاعدة بيانات إيجارات بندر
// تسمح بالقراءة والكتابة فقط للمستخدم المسجل دخوله (أنت)
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
